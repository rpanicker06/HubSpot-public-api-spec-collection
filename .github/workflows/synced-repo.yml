name: Sync Forked Repo

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2
        with:
          persist-credentials: true

      - name: Set up Git
        run: |
          git config --global user.name 'hseligson1'
          git config --global user.email 'hseligson@gmail.com'

      - name: Add upstream repository
        run: git remote add upstream https://github.com/HubSpot/HubSpot-public-api-spec-collection

      - name: Fetch upstream
        run: git fetch upstream

      - name: Merge upstream changes
        run: |
          git checkout main
          git merge upstream/main --strategy=ort
          git push origin main
        env:
          PAT: ${{ secrets.PAT }}
